# Operation - UI仕様書

## 📋 セクション概要

### セクション名
**Operation (オペレーション)**

### 所属サービス
**flow-auditor** の **operation** セクションに配置

### セクションの目的
フロー全体の制御と状態更新を行うUI。[更新][リトライ]ボタンによる能動的な進捗コントロールを提供する。

---

## 🖼️ レイアウト設計

### ワイヤーフレーム
```
┌────────────────────────────────────────────────┐
│ Operation Section                              │
│ ┌────────────────────────────────────────────┐│
│ │ サービス: [▼ flow-auditor        ]        ││
│ │ [🔄 更新] [⏮️ リトライ]    最終更新: 14:32 ││
│ └────────────────────────────────────────────┘│
└────────────────────────────────────────────────┘
```

### コンポーネント階層
- **OperationSection** (親コンポーネント)
  - **ServiceSelector**: サービス選択プルダウン（NEW）
  - **RefreshButton**: 更新ボタン
  - **RetryButton**: リトライボタン
  - **LastUpdatedLabel**: 最終更新日時表示
  - **RetryModal** (条件付き表示)
    - RetryTargetSelector: リトライ対象選択（project.toml / セクション設計 / 実施フロー）
    - StepSelector: ステップ選択（ドロップダウン、条件付き表示）
    - ConfirmButton: 実行ボタン
    - CancelButton: キャンセルボタン

---

## 🎨 UIコンポーネント仕様

### 1. ServiceSelector (サービス選択プルダウン) **NEW**

**責務**: project.tomlに登録された全サービスから監査対象を選択する。

#### ステップ1: モック実装 (UIの確立)
**Props (インターフェース)**:
```typescript
interface ServiceSelectorProps {
  services: string[];
  selectedService: string;
  onChange: (serviceName: string) => void;
}
```
**表示ロジック**:
- ラベル「サービス:」と、渡された`services`配列をオプションに持つプルダウンを表示する。
- `selectedService` propに一致するオプションが選択された状態になる。

#### ステップ2: 機能強化 (ロジックの接続)
**インタラクション**:
- ユーザーがプルダウンの選択を変更すると、`onChange`ハンドラが発火する。
**データ連携**:
- `loader`から取得したサービス名一覧を`services` propとして受け取る。

---

### 2. RefreshButton (更新ボタン)

**責務**: 全チェックポイントのファイル存在を再確認し、最新の進捗状態を取得する。

#### ステップ1: モック実装 (UIの確立)
**Props (インターフェース)**:
```typescript
interface RefreshButtonProps {
  isLoading: boolean;
  onRefresh: () => void;
}
```
**表示ロジック**:
- テキスト「🔄 更新」を持つボタンを表示する。
- `isLoading`が`true`の場合、ボタンを無効化し、スピナーを表示する。

#### ステップ2: 機能強化 (ロジックの接続)
**インタラクション**:
- ボタンをクリックすると`onRefresh`ハンドラが発火し、`refresh`アクションを送信する。

---

### 3. RetryButton (リトライボタン)

**責務**: design-flowセクションで選択中のチェックポイントからリトライ確認モーダルを開く。

#### ステップ1: モック実装 (UIの確立)
**Props (インターフェース)**:
```typescript
interface RetryButtonProps {
  isDisabled: boolean;
  onOpenModal: () => void;
}
```
**表示ロジック**:
- テキスト「⏮️ リトライ」を持つボタンを表示する。
- `isDisabled`が`true`の場合、ボタンを無効化し、ツールチップ「チェックポイントを選択してください」を表示する。

#### ステップ2: 機能強化 (ロジックの接続)
**データ連携**:
- URLのSearch Paramsから`selectedCheckpointId`を取得し、それが存在しない場合に`isDisabled`を`true`にする。
**インタラクション**:
- ボタンが有効な状態でクリックされると、`onOpenModal`ハンドラが発火する。

---

### 4. LastUpdatedLabel (最終更新日時表示)

**責務**: 最後に[更新]を実行した日時を表示する。

#### ステップ1: モック実装 (UIの確立)
**Props (インターフェース)**:
```typescript
interface LastUpdatedLabelProps {
  lastUpdated: Date;
}
```
**表示ロジック**:
- `lastUpdated` propを元に、「最終更新: HH:MM」形式で日時を表示する。
- 1分以内の場合は「最終更新: たった今」と表示する。

#### ステップ2: 機能強化 (ロジックの接続)
**データ連携**:
- `loader`から取得した最終更新日時を`lastUpdated` propとして受け取る。

---

### 5. RetryModal (リトライモーダル) **UPDATED**

**責務**: 選択中のチェックポイントからリトライする際の確認を行う。

#### ステップ1: モック実装 (UIの確立)
**Props (インターフェース)**:
```typescript
interface RetryModalProps {
  isOpen: boolean;
  checkpointLabel: string;
  affectedFiles: string[];
  onConfirm: () => void;
  onCancel: () => void;
}
```
**表示ロジック**:
- `isOpen`が`true`の場合にモーダルを表示する。
- `checkpointLabel`と`affectedFiles`のリストを表示する。
```
┌──────────────────────────────────────────────┐
│ ⏮️ フローをリトライ                          │
├──────────────────────────────────────────────┤
│ リトライ開始地点:                            │
│ 📚️ func-spec.md (design-flow)             │
│                                              │
│ ⚠️ 警告: 以下のファイルがアーカイブされます   │
│                                              │
│ □ develop/flow-auditor/design-flow/         │
│   func-spec.md                            │
│ □ develop/flow-auditor/design-flow/         │
│   uiux-spec.md                        │
│ □ develop/flow-auditor/design-flow/         │
│   spec.yaml                                  │
│ □ develop/flow-auditor/design-flow/         │
│   TDD_WORK_FLOW.md                           │
│ □ ... (implementation-flow全ファイル)        │
│                                              │
│ アーカイブ先:                                │
│ develop/archive/20251009_164712/             │
│                                              │
│ [キャンセル]  [実行]                         │
└──────────────────────────────────────────────┘
```
#### ステップ2: 機能強化 (ロジックの接続)
**データ連携**:
- `selectedCheckpointId`を元に、`loader`から取得したチェックポイント定義から`checkpointLabel`を生成する。
- `selectedCheckpointId`を元に、`retryTargetCalculator`（lib層）を呼び出して`affectedFiles`を計算する。
**インタラクション**:
- [実行]ボタンをクリックすると`onConfirm`ハンドラが発火し、`retry`アクションを送信する。
- [キャンセル]ボタンまたは背景をクリックすると`onCancel`ハンドラが発火し、モーダルを閉じる。

---

## 🔄 データフロー（UI層のみ）

### loaderからの取得データ
```typescript
interface OperationLoaderData {
  designFlowStatus: CheckpointStatus[]
  implementationFlowStatus: CheckpointStatus[]
  lastUpdated: Date
}
```

### actionへの送信データ
```typescript
// 更新アクション
{ _action: 'refresh' }

// リトライアクション
{ _action: 'retry', targetPhase: 'design' | 'implementation', stepIndex: number }
```

---

## 📱 レスポンシブ対応

### モバイル (< 768px)
- ボタンを縦並びに配置
- モーダルを全画面表示

### タブレット (768px - 1024px)
- ボタンを横並びに配置
- モーダルを中央に配置（幅80%）

### デスクトップ (> 1024px)
- ボタンを横並びに配置
- モーダルを中央に配置（幅60%）

---

## 🎨 デザイントークン使用箇所

| 要素 | トークン | 値 |
|:---|:---|:---|
| 更新ボタン背景 | `--flow-auditor-primary` | cyan-400 |
| 更新ボタングロー | `--flow-auditor-glow` | 0 0 10px currentColor |
| リトライボタン背景 | `--flow-auditor-secondary` | green-400 |
| 最終更新テキスト | `--flow-auditor-status-completed` | green-400 |
| モーダル枠線 | `--flow-auditor-primary` | cyan-400 |
| モーダルノード背景 | `background-color` | rgba(0, 0, 0, 0.3) |
| モーダルノードボーダー | `border` | 2px solid var(--flow-auditor-primary) |
| 実行ボタン背景 | `--flow-auditor-status-error` | red-400 |
| フォント | `--flow-auditor-font-family` | 'Courier New', monospace |

### サイバーパンクノードデザイン適用要素
- **RefreshButton**: border-radius: 0, box-shadow: var(--flow-auditor-glow)
- **RetryButton**: border-radius: 0, box-shadow: var(--flow-auditor-glow)
- **RetryModal**: border-radius: 0, 2px solid border, グローエフェクト
- **ServiceSelector**: シャープな角、ボーダーグロー

---

## ♿ アクセシビリティ

- **キーボード操作**: Tabキーでボタン間移動、Enterで実行
- **スクリーンリーダー**: `aria-label`で各ボタンの役割を明示
- **コントラスト比**: WCAG AA準拠（4.5:1以上）

---

> **💡 Design Principle**:
> オペレーションセクションは「司令塔」として、常に画面上部に固定配置し、即座にアクセス可能にする。

> **🎯 UX Goal**:
> [更新]は1クリック、[リトライ]は3クリック（開く→選択→実行）で完結し、操作の手間を最小化する。
