# authentication - 機能設計書

## 📋 機能概要

### 機能名

Authentication (認証)

### 所属サービス

**account** の **authentication** セクションに配置

### 機能の目的・価値

- **解決する課題**: ユーザーの本人確認と、セキュアなアカウントアクセスを実現する
- **提供する価値**:
  - メールアドレス/パスワードによる安全な会員登録・ログイン機能
  - セッションベースの認証状態管理
  - セキュアなログアウト処理
- **ビジネス効果**: ユーザーアカウントの保護、不正アクセス防止、ユーザー体験の向上

### 実装優先度

**HIGH** - `common`セクション完了後、最優先で実装（profile、subscriptionが依存）

## 🎯 機能要件

### 基本機能

#### 1. 会員登録 (Registration)

**URL**: `/register`

**機能**:
- 新規ユーザーアカウントの作成
- メールアドレス/パスワードによる登録
- パスワードのハッシュ化と安全な保存
- 登録成功時のセッション自動生成とログイン状態への遷移

**入力データ**:
- メールアドレス
- パスワード
- パスワード確認（再入力）

**処理フロー**:
1. 入力バリデーション（メール形式、パスワード強度チェック）
2. メールアドレスの重複チェック
3. パスワードのハッシュ化
4. ユーザーレコードの作成（D1 Database）
5. セッション生成（commonのユーティリティ使用）
6. Cookie設定とリダイレクト（`/account`へ）

**出力データ**:
- 成功時: セッションCookieを発行し、`/account`へリダイレクト
- 失敗時: エラーメッセージを表示（フォーム上）

**エラーハンドリング**:
- メールアドレス重複: 「このメールアドレスは既に登録されています」
- パスワード不一致: 「パスワードが一致しません」
- バリデーション失敗: 各フィールドにエラーメッセージ表示

#### 2. ログイン (Login)

**URL**: `/login`

**機能**:
- 既存ユーザーの認証とセッション生成
- メールアドレス/パスワードによる認証
- リダイレクトURL対応（`?redirect-url=/account/settings`形式）

**入力データ**:
- メールアドレス
- パスワード
- リダイレクトURL（オプション、クエリパラメータ）

**処理フロー**:
1. 入力バリデーション
2. ユーザー検索（メールアドレスで照会）
3. パスワード検証（ハッシュ比較）
4. セッション生成（commonのユーティリティ使用）
5. Cookie設定とリダイレクト（指定URLまたは`/account`へ）

**出力データ**:
- 成功時: セッションCookieを発行し、指定URLへリダイレクト
- 失敗時: エラーメッセージを表示

**エラーハンドリング**:
- 認証失敗: 「メールアドレスまたはパスワードが正しくありません」
  - **セキュリティ原則**: どちらが間違っているか特定させない（列挙攻撃対策）
- アカウントロック（将来実装）: 「アカウントがロックされています」

#### 3. ログアウト (Logout)

**URL**: `/logout`（actionのみ、画面なし）

**機能**:
- セッションの破棄とログアウト処理
- Cookie無効化

**入力データ**:
- なし（現在のセッションCookieを使用）

**処理フロー**:
1. セッションID取得（Cookie）
2. セッション削除（Cloudflare Workers KV）
3. Cookie無効化（`Max-Age=0`設定）
4. `/login`へリダイレクト

**出力データ**:
- Cookie削除ヘッダー
- `/login`へリダイレクト

## 📂 app/components要件

### UI Components

#### 1. RegisterForm

**配置**: `app/components/account/authentication/RegisterForm.tsx`

**責務**:
- 会員登録フォームの表示
- バリデーションエラーの表示
- 送信処理

**主要なUI要素**:
- メールアドレス入力（FormField使用）
- パスワード入力（FormField使用）
- パスワード確認入力（FormField使用）
- 登録ボタン（Button使用）
- エラーメッセージ表示エリア（ErrorMessage使用）
- ログインリンク（既存アカウントがある場合）

**状態管理**:
- フォーム送信中フラグ（ボタンのローディング状態制御）
- バリデーションエラー表示

#### 2. LoginForm

**配置**: `app/components/account/authentication/LoginForm.tsx`

**責務**:
- ログインフォームの表示
- バリデーションエラーの表示
- 送信処理

**主要なUI要素**:
- メールアドレス入力（FormField使用）
- パスワード入力（FormField使用）
- ログインボタン（Button使用）
- エラーメッセージ表示エリア（ErrorMessage使用）
- パスワードリセットリンク（将来実装）
- 会員登録リンク

**状態管理**:
- フォーム送信中フラグ
- バリデーションエラー表示

### Routes

#### 1. register.tsx

**配置**: `app/routes/register.tsx`

**責務**:
- 会員登録ページのRoute定義
- loader: 既にログイン済みの場合は`/account`へリダイレクト
- action: 会員登録処理の実行

**loader処理**:
- セッション確認
- 認証済みなら`/account`へリダイレクト
- 未認証ならnull返却（フォーム表示）

**action処理**:
1. FormDataから入力値取得
2. バリデーション実行
3. メールアドレス重複チェック
4. ユーザー作成
5. セッション生成
6. Cookie設定してリダイレクト

#### 2. login.tsx

**配置**: `app/routes/login.tsx`

**責務**:
- ログインページのRoute定義
- loader: 既にログイン済みの場合は`/account`へリダイレクト
- action: ログイン処理の実行

**loader処理**:
- セッション確認
- 認証済みなら`/account`へリダイレクト
- 未認証ならnull返却（フォーム表示）

**action処理**:
1. FormDataから入力値取得
2. バリデーション実行
3. 認証処理（メール/パスワード検証）
4. セッション生成
5. Cookie設定してリダイレクト（redirect-urlパラメータ優先）

#### 3. logout.tsx

**配置**: `app/routes/logout.tsx`

**責務**:
- ログアウト処理専用Route（画面なし）
- action: セッション破棄とリダイレクト

**action処理**:
1. セッションID取得
2. セッション削除（KV）
3. Cookie無効化
4. `/login`へリダイレクト

## 🧠 純粋ロジック要件

### lib層の関数

#### 1. hashPassword

**配置**: `app/lib/account/authentication/hashPassword.ts`

**責務**: パスワードのハッシュ化

**入力**: 平文パスワード（string）

**処理**: bcryptを使用してハッシュ化（salt rounds: 10）

**出力**: ハッシュ化されたパスワード（string）

#### 2. verifyPassword

**配置**: `app/lib/account/authentication/verifyPassword.ts`

**責務**: パスワードの検証

**入力**:
- 平文パスワード（string）
- ハッシュ化されたパスワード（string）

**処理**: bcrypt.compareを使用して比較

**出力**: boolean（一致/不一致）

#### 3. validateRegistration

**配置**: `app/lib/account/authentication/validateRegistration.ts`

**責務**: 会員登録フォームのバリデーション

**入力**:
- メールアドレス
- パスワード
- パスワード確認

**処理**:
- メール形式チェック（正規表現）
- パスワード強度チェック（最小長、大文字小文字数字含む）
- パスワード一致チェック
- 必須項目チェック

**出力**: バリデーションエラーの配列（エラーがない場合は空配列）

#### 4. validateLogin

**配置**: `app/lib/account/authentication/validateLogin.ts`

**責務**: ログインフォームのバリデーション

**入力**:
- メールアドレス
- パスワード

**処理**:
- メール形式チェック
- 必須項目チェック

**出力**: バリデーションエラーの配列

## 🔌 副作用要件

### data-io層の関数

#### 1. createUser.server.ts

**配置**: `app/data-io/account/authentication/createUser.server.ts`

**責務**: 新規ユーザーのDB登録

**入力**:
- メールアドレス
- ハッシュ化されたパスワード

**処理**:
1. D1 Databaseにユーザーレコード挿入
2. デフォルト値設定（subscriptionStatus: 'inactive', createdAt, updatedAt）

**出力**: 作成されたユーザー情報（User型）

**エラーハンドリング**:
- メールアドレス重複時: UNIQUE制約エラーをキャッチ

#### 2. findUserByEmail.server.ts

**配置**: `app/data-io/account/authentication/findUserByEmail.server.ts`

**責務**: メールアドレスでユーザー検索

**入力**: メールアドレス

**処理**: D1 DatabaseでSELECT文実行

**出力**: ユーザー情報（User型）またはnull

#### 3. checkEmailExists.server.ts

**配置**: `app/data-io/account/authentication/checkEmailExists.server.ts`

**責務**: メールアドレスの存在確認

**入力**: メールアドレス

**処理**: D1 DatabaseでCOUNT文実行

**出力**: boolean（存在する/しない）

## 📊 データフロー

### 会員登録フロー

```
ユーザー入力
    ↓
RegisterForm (UI)
    ↓
register.tsx action
    ↓
validateRegistration (lib) - バリデーション
    ↓
checkEmailExists.server (data-io) - 重複チェック
    ↓
hashPassword (lib) - パスワードハッシュ化
    ↓
createUser.server (data-io) - DB登録
    ↓
createSessionData (lib/common) - セッション生成
    ↓
saveSession.server (data-io/common) - セッション保存
    ↓
Cookie設定 + /account へリダイレクト
```

### ログインフロー

```
ユーザー入力
    ↓
LoginForm (UI)
    ↓
login.tsx action
    ↓
validateLogin (lib) - バリデーション
    ↓
findUserByEmail.server (data-io) - ユーザー検索
    ↓
verifyPassword (lib) - パスワード検証
    ↓
createSessionData (lib/common) - セッション生成
    ↓
saveSession.server (data-io/common) - セッション保存
    ↓
Cookie設定 + リダイレクト
```

### ログアウトフロー

```
ログアウトリクエスト
    ↓
logout.tsx action
    ↓
getSession.server (data-io/common) - セッションID取得
    ↓
destroySession.server (data-io/common) - セッション削除
    ↓
Cookie無効化 + /login へリダイレクト
```

## 🔒 セキュリティ要件

### 1. パスワードセキュリティ

- **ハッシュ化アルゴリズム**: bcrypt（`common-spec.yaml`で定義）
- **Salt Rounds**: 10
- **平文保存禁止**: パスワードは必ずハッシュ化してから保存

### 2. セッション管理

- **Cookie設定**:
  - HttpOnly: true（JavaScriptからアクセス不可）
  - Secure: true（HTTPS必須）
  - SameSite: Lax（CSRF対策）
- **セッション期限**: 7日間（`common-spec.yaml`で定義）

### 3. 認証エラーメッセージ

- **原則**: 具体的な失敗理由を開示しない
- **NG**: 「パスワードが間違っています」「このメールアドレスは存在しません」
- **OK**: 「メールアドレスまたはパスワードが正しくありません」

### 4. CSRF対策（将来実装）

- CSRFトークンの検証（`common-spec.yaml`の`security.csrf`設定に従う）

## 🧪 テスト要件

### E2Eテスト

**配置**: `tests/e2e/section/account/authentication.spec.ts`

**テストケース**:
- 会員登録の成功シナリオ
- メールアドレス重複時のエラー表示
- ログインの成功シナリオ
- 認証失敗時のエラー表示
- ログアウトの成功シナリオ

### 単体テスト

各関数ごとにテストファイルを作成：
- `hashPassword.test.ts`
- `verifyPassword.test.ts`
- `validateRegistration.test.ts`
- `validateLogin.test.ts`
- `createUser.server.test.ts`（DBモック使用）
- `findUserByEmail.server.test.ts`（DBモック使用）

## 🚀 実装の優先順位

**Phase 1**: 基本認証機能
1. 会員登録（register.tsx, RegisterForm）
2. ログイン（login.tsx, LoginForm）
3. ログアウト（logout.tsx）

**Phase 2**: セキュリティ強化（将来実装）
- パスワードリセット機能
- メール認証（確認メール送信）
- アカウントロック（ログイン試行回数制限）

**Phase 3**: OAuth連携（将来実装）
- Google OAuth
- Apple OAuth

## 📝 備考

### 依存関係

- **commonセクションへの依存**:
  - セッション管理ユーティリティ（createSessionData, saveSession.server, destroySession.server）
  - 共通UIコンポーネント（FormField, Button, ErrorMessage）
  - spec.yamlの設定値（セキュリティ設定、バリデーションルール）

### 外部ライブラリ

- **bcrypt**: パスワードハッシュ化（`npm install bcrypt`、型定義: `@types/bcrypt`）

### データベーススキーマ（D1）

**usersテーブル**:

| カラム名 | 型 | 制約 | 説明 |
|:---|:---|:---|:---|
| id | TEXT | PRIMARY KEY | ユーザーID（UUID） |
| email | TEXT | UNIQUE, NOT NULL | メールアドレス |
| password | TEXT | NOT NULL | ハッシュ化されたパスワード |
| subscriptionStatus | TEXT | NOT NULL | 'active', 'inactive', 'trial' |
| createdAt | TEXT | NOT NULL | 作成日時（ISO 8601） |
| updatedAt | TEXT | NOT NULL | 更新日時（ISO 8601） |

> **注**: 具体的な型定義は `app/specs/account/types.ts` で定義します。

---

**最終更新**: 2025-12-23
