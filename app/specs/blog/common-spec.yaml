# spec.yaml - 外部変数仕様書
# 実装・テスト・仕様書で共通参照するリテラル値定義

# ==========================================
# メタデータ
# ==========================================
metadata:
  feature_name: "blog-common"
  slug: "common"
  created_date: "2025-11-14"
  last_updated: "2025-11-14"
  version: "1.0.0"

# ==========================================
# サーバー処理設定 (Loader/Action)
# ==========================================
server_io:
  loader: {}
  action:
    # このセクションではactionは不要（共通コンポーネントは表示のみ）
    intents: {}

# ==========================================
# ブログ設定定数
# ==========================================
blog_config:
  title: "ClaudeMix Blog"
  home_path: "/blog"
  site_url: "https://stg.claudemix.pages.dev"

# ==========================================
# ナビゲーションメニュー設定
# ==========================================
navigation:
  menu_items:
    - label: "マイページ"
      path: "/account"
    - label: "はじめまして"
      path: "/blog/hazimemasite"
    - label: "カテゴリー"
      path: "/blog/welcome"
    - label: "LINK"
      path: "/blog/link"

  # メニューアニメーション設定
  animation:
    fade_duration_ms: 200
    transition_timing: "ease-in-out"

# ==========================================
# フッターリンク設定
# ==========================================
footer:
  # 法的リンク
  legal_links:
    - label: "利用規約"
      href: "/blog/terms"
      is_modal: false
    - label: "プライバシーポリシー"
      href: "/blog/privacy"
      is_modal: false
    - label: "特定商取引法に基づく表記"
      is_modal: true

  # 特定商取引法の内容（モーダル表示用）
  #
  # 【本番環境での設定方法】
  # 個人情報（住所、電話番号など）を含むため、以下の方法で設定してください：
  #
  # 1. このファイル：プレースホルダー（[...]）のまま保持（テンプレート）
  # 2. Cloudflare Pages 環境変数：実際の個人情報を設定
  #    - 変数名: LEGAL_CONTENT
  #    - 値: 以下のHTMLを実際の情報で置き換えたもの
  #
  # 環境変数が設定されている場合、そちらが優先的に使用されます。
  # 開発環境では、このプレースホルダーが使用されます。
  #
  legal_content: |
    <section>
      <h3>販売事業者</h3>
      <p>個人事業主 [事業者名]</p>
    </section>
    <section>
      <h3>運営統括責任者</h3>
      <p>[責任者名]</p>
    </section>
    <section>
      <h3>所在地</h3>
      <p>[住所]</p>
      <p>※個人情報保護のため、具体的な住所はご購入後にメールにてお知らせいたします。</p>
    </section>
    <section>
      <h3>電話番号</h3>
      <p>[電話番号]</p>
      <p>※お問い合わせは原則としてメールにて承っております。</p>
    </section>
    <section>
      <h3>メールアドレス</h3>
      <p>[メールアドレス]</p>
    </section>
    <section>
      <h3>販売価格</h3>
      <p>各商品ページに記載の通りです。</p>
    </section>
    <section>
      <h3>商品代金以外の必要料金</h3>
      <p>消費税、振込手数料</p>
    </section>
    <section>
      <h3>支払方法</h3>
      <p>クレジットカード決済（Stripe）</p>
    </section>
    <section>
      <h3>支払時期</h3>
      <p>ご購入時に即時決済されます。</p>
    </section>
    <section>
      <h3>商品の引渡時期</h3>
      <p>決済完了後、即時アクセス可能となります。</p>
    </section>
    <section>
      <h3>返品・キャンセルについて</h3>
      <p>デジタルコンテンツの性質上、原則として返品・返金はお受けできません。ただし、商品に瑕疵がある場合はこの限りではありません。</p>
    </section>

# ==========================================
# UI要素セレクタ
# ==========================================
ui_selectors:
  layout:
    blog_layout: "[data-testid='blog-layout']"
    main_content: "[data-testid='main-content']"

  header:
    blog_header: "[data-testid='blog-header']"
    title_area: "[data-testid='title-area']"
    title_link: "[data-testid='title-link']"
    header_actions: "[data-testid='header-actions']"
    theme_toggle_button: "[data-testid='theme-toggle-button']"
    menu_button: "[data-testid='menu-button']"

  navigation:
    navigation_menu: "[data-testid='navigation-menu']"
    menu_item: "[data-testid='menu-item']"
    menu_overlay: "[data-testid='menu-overlay']"

  footer:
    blog_footer: "[data-testid='blog-footer']"
    copyright: "[data-testid='copyright']"
    footer_links: "[data-testid='footer-links']"
    footer_link: "[data-testid^='footer-link-']"

  legal_modal:
    modal_overlay: "[data-testid='legal-modal-overlay']"
    modal_dialog: "[role='dialog']"
    modal_title: "#legal-modal-title"
    close_button: "[data-testid='close-button']"

# ==========================================
# レスポンシブ設定
# ==========================================
responsive:
  menu_display_mode:
    mobile: "modal" # 全画面モーダル形式
    tablet: "dropdown" # ドロップダウン形式
    desktop: "dropdown" # ドロップダウン形式

# ==========================================
# アクセシビリティ (ARIA)
# ==========================================
accessibility:
  aria_labels:
    menu_button: "メニューを開く"
    menu_button_open: "メニューを閉じる"
    navigation_menu: "ナビゲーションメニュー"
    close_menu: "メニューを閉じる"
    home_link: "ホームに戻る"
    theme_toggle_button: "テーマを切り替え"
    theme_toggle_button_light: "ダークモードに切り替え"
    theme_toggle_button_dark: "ライトモードに切り替え"
    legal_modal_close: "閉じる"

  keyboard_shortcuts:
    close_menu: "Escape"
    close_modal: "Escape"

# ==========================================
# テーマ設定
# ==========================================
theme:
  # テーマの種類
  modes:
    - "light"
    - "dark"

  # デフォルトテーマ
  default_mode: "dark"

  # localStorage設定
  storage:
    key: "theme"

  # HTML属性
  html_attribute:
    name: "data-theme"
    light_value: "light"
    dark_value: "dark"

  # アイコン表示
  icons:
    light: "☀️"  # ライトモード時に表示（太陽アイコン）
    dark: "🌙"   # ダークモード時に表示（月アイコン）

# ==========================================
# OGP画像生成設定
# ==========================================
ogp:
  image:
    width: 1200
    height: 630
    format: "png"
  cache:
    maxAge: 31536000  # 1年 (秒)
    directive: "public, max-age=31536000, immutable"

  # レイアウト設定
  layout:
    paddingX: "80px"
    paddingY: "60px"
    contentGap: "24px"

  # 色設定（サイトデザイントークンと統一）
  colors:
    background:
      gradientStart: "#0a0a0a"  # var(--color-surface-dark)
      gradientEnd: "#1a1a1a"    # var(--color-background-secondary)
      gradientAngle: "135deg"
    text:
      primary: "#22d3ee"        # var(--color-interactive-primary) - タイトル
      description: "#d4d4d4"    # var(--color-text-secondary) - 説明
      author: "#b9b6b6"         # var(--color-text-primary) - 著者

  # テキスト設定
  title:
    maxLength: 60
    fontSize: 48
    maxLines: 2
  description:
    maxLength: 120
    fontSize: 24
    maxLines: 3
  author:
    fontSize: 16
    prefix: "By "

  # フォント設定
  font:
    name: "Noto Sans JP"  # workers-ogに登録する単一フォント名
    family: "'Noto Sans JP', 'Hiragino Sans', 'ヒラギノ角ゴ ProN W3', 'メイリオ', Meiryo, sans-serif"

    # フォント取得設定（Data-IO層で使用）
    fetch:
      apiUrl: "https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400&display=swap"
      userAgent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"
      cacheName: "ogp-fonts-v1"
      contentType: "font/ttf"
      cacheControl: "public, max-age=31536000, immutable"
      urlRegex: "url\\((https:\\/\\/[^)]+\\.ttf)\\)"
