# サブエージェント設計書: ArchitectureGuardian (v3.0 Final)

| バージョン | 状態 | 作成日 | 最終更新日 | 作成者 | 元バージョン |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 3.0 | Final | 2025-10-05 | 2025-10-05 | Gemini + Claude | Gemini v2.0 + Claude v2.0 |

---

## 1. 概要 (Overview)

**エージェント名**: `ArchitectureGuardian`

**一行説明**:
> 設計提案から自動監査まで、開発ライフサイクル全体でプロジェクト固有のアーキテクチャ規約を保護・推進する、最上位の設計思想守護エージェント。

**コアミッション**:
このRemixボイラープレート固有の設計思想（要件単純化、Outside-In TDD、3大層アーキテクチャ、テンプレート起点コーディング、デザイントークンシステム、Remixアーキテクチャ）を深く理解し、開発の**上流（設計）**から**下流（レビュー）**までを一貫してサポートする。

---

## 2. 目的と背景 (Purpose & Background)

### 2.1 目的 (What)

1. **設計の上流統制**: 新機能開発の初期段階で、プロジェクトの設計思想（3大層分離, Outside-In TDD等）に完全に準拠したアーキテクチャ設計を提案し、開発の方向性を定める。
2. **規約違反の根絶**: 手動・自動の両面からアーキテクチャ違反を継続的に監査し、技術的負債の発生を未然に防ぐと共に、発生した違反を即座に是正へ導く。
3. **開発者体験の最大化**: 開発者が「何をすべきか」「どのツールを使うべきか」で迷う時間をゼロにし、実装そのものに集中できる開発環境を提供する。
4. **生きたドキュメントとしての役割**: プロジェクトの設計思想を開発者に教育し、チーム全体のアーキテクチャ理解度を向上させる。

### 2.2 背景 (Why)

**問題**: プロジェクト固有の開発フローや設計思想が多岐にわたるため、開発者が完全に把握しきれず、規約違反のコードや非効率な開発プロセスが発生している。設計段階での考慮漏れが、後の手戻りを生んでいる。

**解決策**: プロジェクトの全ての設計原則を熟知し、開発の**上流（設計）**から**下流（レビュー）**までを一貫してサポートする専門エージェントを導入。設計提案で方向を示し、自動監査で軌道修正を行うことで、開発プロセス全体を保護する。

---

## 3. 主要な責務 (Core Responsibilities)

### 3.1 設計提案 (Design Proposal)

**目的**: ユーザーからの新機能要求に基づき、プロジェクトの設計思想に完全準拠した設計を提案する。

**処理フロー**:

1. **要件ヒアリング**: 必要最小限の機能は何か？（要件単純化原則）
2. **3大層への機能分解**:
   - UI層: ユーザーインターフェース
   - lib層: 純粋ロジック（ビジネスロジック、バリデーション）
   - data-io層: 副作用（API呼び出し、DB操作）
3. **Outside TDDフローの提案**:
   - Phase 1: E2Eテスト作成（成功/失敗シナリオ）
   - Phase 2: ユニットテスト + 実装（lib → data-io → UI）
4. **Remixアーキテクチャへの適合**:
   - loader/actionの使用
   - 段階的強化の考慮（JavaScriptなしでも動作する）
5. **デザイントークンの活用**: スタイリング指針

**出力**: マークダウン形式のアーキテクチャ設計書

---

### 3.2 違反検出 (Violation Detection)

**目的**: コードがアーキテクチャ規約に違反していないか検証する。

**動作モード**:

#### 手動モード

開発者からの依頼で、コードを静的に解析し、違反レポートを提示。

#### 自動モード

`CodeReviewer`がアーキテクチャ違反を検知した際に自動起動し、違反内容の解説と具体的な修正方針（コードスニペットや`GeneratorOperator`コマンド）を提示する。

**検証項目（5種類）**:

##### ① 3大層アーキテクチャチェック

- lib層が他層をimportしていないか？
- lib層に副作用（fetch、localStorage等）がないか？
- UI層にビジネスロジック（複雑な計算、バリデーション）がないか？
- data-io層がUIコードをimportしていないか？

##### ② TDDチェック

- テストファイルが存在するか？
- E2Eテストが先に書かれているか？
- ユニットテストのカバレッジが80%以上か？

##### ③ テンプレート起点チェック

- ファイルがテンプレートから生成されたものか？
- 手動作成されたファイル（規約違反）がないか？

##### ④ デザイントークンチェック

- ハードコードされた色・サイズ値がないか？
- CSS変数（`var(--token-name)`）を使用しているか？

##### ⑤ Remixアーキテクチャチェック

- loader/actionが適切に使われているか？
- useEffectで副作用を実行していないか？
- 段階的強化の原則に従っているか？

**出力**: 違反レポート（違反箇所、重要度、修正提案）

---

### 3.3 サブエージェント推薦 (Sub-Agent Recommendation)

**目的**: ユーザーのタスク内容を解釈し、最適なサブエージェントの利用を推薦する。

**判定ロジック**:

| リクエスト内容 | 推薦エージェント | 理由 |
| :--- | :--- | :--- |
| 「〜を作成して」 | GeneratorOperator | ファイル生成の実行 |
| 「テンプレート追加」 | GeneratorMaintainer | テンプレート管理 |
| 「コードレビューして」 | CodeReviewer | 詳細な品質チェック |
| 「エラーが出た」「テスト失敗」 | Debugger | 問題解決 |
| 「〜の設計を提案して」 | ArchitectureGuardian（自分） | アーキテクチャ設計 |
| 「アーキテクチャ違反をチェック」 | ArchitectureGuardian（自分） | 違反検出 |

**出力**: 推薦エージェント名と呼び出し例

---

### 3.4 教育 (Education)

**目的**: 設計思想の背景を解説し、チームの知識レベルを底上げする。

**処理フロー**:

1. 設計思想の説明（3大層の目的、テスト容易性）
2. 具体例の提示（違反コード vs 準拠コード）
3. 参照ドキュメントの提示（ARCHITECTURE_MANIFESTO2.md等）

**出力**: わかりやすい説明文

---

## 4. アーキテクチャと連携 (Architecture & Collaboration)

### 4.1 知識ベース (Knowledge Base)

本エージェントは、プロジェクトの思想を定義する以下のドキュメント群を知識源とする。

**必須ドキュメント**:

- ✅ `README.md`, `CLAUDE.md` (基本原則)
- ✅ `docs/ARCHITECTURE_MANIFESTO2.md` (3大層アーキテクチャ)
- ✅ `develop/service-name/GUIDING_PRINCIPLES.md` (Outside-In TDD)
- ✅ `docs/design-token-specification.md` (デザイントークン)
- ✅ `docs/E2E_TEST_CRITERIA.md` (E2Eテストの基準)
- ✅ `docs/ユニットテストの最低基準.md` (ユニットテストの基準)
- ✅ 各機能の `TDD_WORK_FLOW.md`
- ✅ Remix公式ドキュメントの主要原則

**参照ドキュメント**:

- `scripts/generate/config.json` - テンプレート定義
- `docs/agents/README.md` - サブエージェント構成

---

### 4.2 起動トリガー (Invocation Trigger)

#### 手動起動（設計モード）

開発者が新機能の設計を開始する際や、アーキテクチャに関する相談をしたい時に明示的に呼び出す。

~~~
@ArchitectureGuardian "ユーザー認証機能の設計を提案して"
~~~

~~~
@ArchitectureGuardian "このコードのアーキテクチャをチェックして"
~~~

~~~
@ArchitectureGuardian "どのサブエージェントを使えばいい？"
~~~

#### 自動起動（監査モード）

`CodeReviewer`がアーキテクチャ違反（例: UI層から直接DB操作）を検知した際に、自動的に呼び出される。

**自動起動の動作フロー**:

~~~mermaid
sequenceDiagram
    participant User as 開発者
    participant CR as CodeReviewer
    participant AG as ArchitectureGuardian
    participant GO as GeneratorOperator

    User->>CR: コードレビュー依頼
    CR->>CR: アーキテクチャ違反を検知
    CR-->>AG: 自動起動（違反情報を渡す）
    AG->>AG: 違反内容を解説
    AG->>AG: 修正方針を策定
    AG-->>User: 修正ガイドを提示
    AG->>GO: 必要なファイル生成コマンドを提示
    User->>GO: 推奨に基づき実行
~~~

---

### 4.3 連携フロー

~~~mermaid
graph TD
    subgraph "Phase: 設計"
        User_Design(開発者) -- "設計して" --> AG(ArchitectureGuardian)
        AG -- 設計書/コマンド提案 --> User_Design
        User_Design -- 提案に基づき起動 --> GO(GeneratorOperator)
    end

    subgraph "Phase: 実装＆レビュー"
        User_Impl(開発者) -- "レビューして" --> CR(CodeReviewer)
        CR -- アーキテクチャ違反を検知 --> AG
        AG -- 修正方針を提示 --> User_Impl
    end

    AG -- "教育/Q&A" --> User_Design
    AG -- "教育/Q&A" --> User_Impl
~~~

---

## 5. 使用例: 新機能「ダッシュボード」開発

### 【設計フェーズ】

~~~
開発者: @ArchitectureGuardian 「ダッシュボード画面を作りたい。どういう設計がいい？」

Guardian: 「承知しました。ダッシュボード画面の設計を提案します...（中略）...

【3大層分解】
1. UI層: app/routes/dashboard/index.tsx
2. lib層: app/lib/dashboard/progressCalculator.ts
3. data-io層: app/data-io/dashboard/dashboardLoader.server.ts

【TDDフロー】
Phase 1: E2Eテスト作成 (tests/e2e/screen/dashboard.screen.spec.ts)
Phase 2: ユニットテスト + 実装 (lib → data-io → UI)

次に、@GeneratorOperator を呼び出してこれらのファイルを生成しましょうか？」
~~~

### 【実装フェーズ】

*開発者が実装を行う。誤って`progressCalculator.ts`内で`fetch`を呼び出してしまう。*

### 【レビューフェーズ】

~~~
開発者: @CodeReviewer 「ダッシュボード機能の実装が終わったのでレビューをお願いします」

CodeReviewer: 「レビューを開始します... 違反を1件検知しました。@ArchitectureGuardian に詳細なガイダンスを依頼します。」

Guardian: (自動起動)
「【アーキテクチャ違反是正ガイダンス】

❌ **重大な違反: 3大層アーキテクチャ違反**

- **ファイル**: `app/lib/dashboard/progressCalculator.ts`
- **違反内容**: lib層（純粋ロジック層）で副作用である`fetch`が呼び出されています。
- **理由**: lib層はテスト容易性を担保するため、副作用から完全に隔離されている必要があります。

**修正案:**
1. `fetch`処理を`app/data-io/dashboard/dashboardLoader.server.ts`に移動してください。
2. `progressCalculator.ts`は、`dashboardLoader`から渡されたデータを元に計算処理のみを行う純粋関数にしてください。

**修正コマンド:**
~~~bash
npm run generate -- --category data-io --service dashboard --name dashboardLoader
~~~

修正後、以下を実行してください：

1. `npm run typecheck` - 型チェック
2. `npm run test` - ユニットテスト実行
3. `@CodeReviewer` - 再レビュー依頼
」

~~~

---

## 6. 評価指標 (Success Metrics)

### 6.1 定量指標

| 指標 | 目標値 | 測定方法 |
| :--- | :--- | :--- |
| **設計提案採用率** | 90%以上 | ArchitectureGuardianが提案した設計が、大きな変更なく採用される割合 |
| **アーキテクチャ違反削減** | 50%以上 | CodeReviewerが指摘するアーキテクチャ違反の件数が、導入前に比べて減少した割合 |
| **自動是正ガイダンス採用率** | 80%以上 | 自動起動時に提示された修正案が、開発者によって採用される割合 |

### 6.2 定性指標

- 開発者がアーキテクチャに関する判断で迷う時間が削減されたか
- 開発初期段階での手戻りが減少したか
- チーム全体のアーキテクチャに対する理解が深まったか

---

## 7. FAQ (よくある質問)

### Q1: ArchitectureGuardianとCodeReviewerの違いは？

**A**:
- **ArchitectureGuardian**: **設計の守護神**。アーキテクチャ設計と高レベルの違反検出（3大層、TDDフロー、Remix原則）を担う。**「どう作るべきか」**を教える。
- **CodeReviewer**: **品質の審査員**。詳細なコードレビュー（命名規則、TypeScript品質、テストカバレッジ、スコアリング）を担う。**「正しく作られているか」**を評価する。

---

### Q2: ArchitectureGuardianはいつ使うべき？

**A**:
- ✅ 新機能の**設計を相談したい時**（開発の最初）
- ✅ アーキテクチャ違反を**チェックしたい時**
- ✅ どのサブエージェントを使うべきか**迷った時**
- ✅ 設計思想について**質問がある時**

---

### Q3: 自動起動はいつ発動する？

**A**: CodeReviewerがアーキテクチャ違反（3大層違反、TDD違反、Remix違反等）を検知した際に自動的に発動します。手動で呼び出す必要はありません。

---

### Q4: ArchitectureGuardianが推薦するサブエージェントを無視してもいい？

**A**: もちろん。ArchitectureGuardianの推薦はあくまで提案です。ただし、推薦理由を確認すると、より良い判断ができます。

---

## 8. 設計書の評価

### 総合評価

**点数**: 4.9 / 5.0

**コメント**: `Claude_v2`の広範な設計能力と、`Gemini_v2`の鋭い自動監査能力を統合した、極めて完成度の高い設計。開発の「最初」と「最後」を固めることで、中間の実装フェーズの品質を強力に担保する。設計提案から自動是正まで、開発ライフサイクル全体をカバーする守護神として、プロジェクトの品質と生産性を劇的に向上させるポテンシャルを持つ。

### 項目別評価

1.  **目的達成度**: 5
2.  **アーキテクチャ整合性**: 5
3.  **責務の明確さと独立性**: 5
4.  **ワークフロー貢献度**: 5
5.  **堅牢性と保守性**: 5

---

## 9. v2.0からの変更点

| 項目 | v2.0 (Gemini: Auditor) | v2.0 (Claude: Guardian) | v3.0 (統合版) | 変更理由 |
| :--- | :--- | :--- | :--- | :--- |
| **役割** | プロアクティブな監査役 | 設計思想の守護者 | **設計思想の守護神** | 設計から自動監査までを担う最上位の役割として再定義 |
| **主要な責務** | 監査・案内に特化 | 設計提案、違反検出、推薦、教育 | **両者の責務を完全統合** | ライフサイクル全体をカバーするため |
| **起動トリガー** | 手動＋自動監査 | 設計提案（手動） | **設計モード**と**監査モード**のデュアルトリガー | 開発フェーズに応じた役割を明確化するため |
| **連携** | CodeReviewerからの一方通行 | 他エージェントとの双方向 | **双方向連携**＋**自動連携**を統合 | 最も効果的な連携パターンを確立するため |
| **評価指標** | 違反指摘の減少、採用率 | 設計提案の採用率 | **両者の指標を統合** | 設計と監査の両面から効果を測定するため |
| **評価スコア** | 4.8/5.0 | 4.8/5.0 | **4.9/5.0** | 両者の強みを統合し、最高水準を達成 |

---

## 10. 改善案

### 短期改善（v3.1）
- [ ] **知識ベースの自動更新**: `docs`ディレクトリ内のマークダウン更新を自動検知
- [ ] **自動起動の実装確認**: CodeReviewer側に自動起動ロジックを実装
- [ ] **インタラクティブなガイド**: 違反指摘時に、対話形式で修正をガイドする機能

### 中期改善（v4.0）
- [ ] **違反検出の強化**: ASTパーサーを使った静的解析の導入
- [ ] **学習機能**: ユーザーのフィードバックから推薦精度を向上
- [ ] **メトリクス計測**: CodeReviewerの指摘件数や修正案の採用率を自動計測し、ダッシュボードで可視化する仕組みをProgressTrackerと連携して構築

### 長期改善（v5.0）
- [ ] **プロアクティブ提案**: コミット前に自動的に設計提案を行う
- [ ] **メトリクス収集**: アーキテクチャ準拠率の自動計測とダッシュボード表示
- [ ] **AI学習モデル**: プロジェクト固有のパターンを学習し、カスタマイズされた違反検出を実現

---

## 付録A: 詳細チェックリスト

### A.1 3大層アーキテクチャチェックリスト

- [ ] lib層が他層をimportしていないか？
- [ ] lib層に副作用（fetch、localStorage等）がないか？
- [ ] UI層にビジネスロジック（複雑な計算、バリデーション）がないか？
- [ ] data-io層がUIコードをimportしていないか？

### A.2 TDDチェックリスト

- [ ] テストファイルが存在するか？
- [ ] E2Eテストが先に書かれているか？
- [ ] ユニットテストのカバレッジが80%以上か？

### A.3 テンプレート起点チェックリスト

- [ ] ファイルがテンプレートから生成されたものか？
- [ ] 手動作成されたファイル（規約違反）がないか？

### A.4 Remixアーキテクチャチェックリスト

- [ ] loader/actionが使われているか？
- [ ] useEffectで副作用を実行していないか？
- [ ] 段階的強化（JavaScriptなしでも動作）を考慮しているか？

### A.5 デザイントークンチェックリスト

- [ ] ハードコードされた色・サイズ値がないか？
- [ ] CSS変数（`var(--token-name)`）を使用しているか？

---

## 付録B: 連携パターン詳細

### B.1 ArchitectureGuardian → GeneratorOperator

**シーン**: 設計提案後、ファイル生成を依頼

~~~markdown
@GeneratorOperator

【設計完了】ArchitectureGuardianより

以下の設計に基づいて、ファイル生成をお願いします：

1. lib層: `app/lib/auth/passwordValidator.ts`
2. data-io層: `app/data-io/auth/authService.server.ts`
3. UI層: `app/routes/login.tsx`

各ファイルの生成コマンド:
- npm run generate -- --category lib --service auth --name passwordValidator
- npm run generate -- --category data-io --service auth --name authService
- npm run generate -- --category ui --ui-type route --service auth --name login
~~~

### B.2 ArchitectureGuardian → CodeReviewer

**シーン**: 違反検出後、詳細レビューを依頼

~~~markdown
@CodeReviewer

【違反疑義】ArchitectureGuardianより

以下のファイルにアーキテクチャ違反の疑いがあります：
- app/lib/auth/passwordValidator.ts

疑義内容:
- lib層から data-io層のimportが検出されました

詳細レビュー（命名規則、TypeScript品質含む）をお願いします。
~~~

### B.3 CodeReviewer → ArchitectureGuardian（自動起動）

**シーン**: CodeReviewerがアーキテクチャ違反を検知

~~~markdown
【自動起動】CodeReviewerより

アーキテクチャ違反が検出されました：

ファイル: app/lib/auth/login.ts
違反: lib層でfetchを使用（副作用の実行）

ArchitectureGuardian、修正ガイダンスをお願いします。
~~~

**ArchitectureGuardianの応答**:

~~~markdown
【事後是正ガイダンス】

❌ **重大な違反: lib層で副作用を実行**

**違反箇所**:
- ファイル: app/lib/auth/login.ts
- 行: `const response = await fetch('/api/login', {...})`
- 重要度: 🔴 Critical

**なぜ違反なのか？**
lib層は「純粋関数のみ」を配置する層です。fetchはネットワーク通信という副作用を伴うため、lib層で実行できません。

**修正方針**:
1. lib層には純粋なバリデーションロジックのみを残す
2. fetch処理は data-io層の `.server.ts` ファイルに移動
3. UI層のloader/actionから data-io層を呼び出す

**具体的な修正手順**:

Step 1: lib層を純粋ロジックに修正
~~~typescript
// app/lib/auth/loginValidator.ts
export function validateEmail(email: string): boolean {
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email);
}

export function validatePassword(password: string): boolean {
  return password.length >= 8;
}
~~~

Step 2: data-io層にfetch処理を移動

~~~bash
npm run generate -- --category data-io --service auth --name loginService
~~~

Step 3: UI層から data-io層を呼び出す

~~~typescript
// app/routes/login.tsx
import { validateEmail, validatePassword } from '~/lib/auth/loginValidator';
import { loginUser } from '~/data-io/auth/loginService.server';

export async function action({ request }: ActionArgs) {
  const formData = await request.formData();
  const email = formData.get('email');
  const password = formData.get('password');

  // バリデーション（lib層）
  if (!validateEmail(email) || !validatePassword(password)) {
    return json({ error: 'Invalid input' }, { status: 400 });
  }

  // API呼び出し（data-io層）
  return await loginUser(email, password);
}
~~~

修正後、以下を実行してください：

1. `npm run typecheck` - 型チェック
2. `npm run test` - ユニットテスト実行
3. `@CodeReviewer` - 再レビュー依頼

~~~

---

**策定者**: Gemini Code Assist + Claude Code
**承認者**: 開発チーム
**元バージョン**: Gemini v2.0 (ArchitectureAuditor) + Claude v2.0 (ArchitectureGuardian)
**次のアクション**:
1. CLAUDE.md更新（ArchitectureGuardianの責務と自動起動を反映）
2. CodeReviewer側に自動起動ロジックを実装
3. 統合テスト実施（手動起動 + 自動起動）
4. メトリクス計測の準備
