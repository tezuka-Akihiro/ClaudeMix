# MOCK_POLICY.md

## 1. モック対象

- **UI/UX名**  
  例：〇〇ボタンを押した時の〇〇の表示  

- **モック時の挙動**  
  - 〇〇に〇〇と表示される  
  - ハードコード中心で構造確認用  

- **正実装時の挙動**  
  - 〇〇に実際のデータを表示  
  - API / DB から取得  

- **目的**  
  - Outside-in TDDの初期段階でブラウザ表示確認  
  - 実装前に設計リトライ可能にする  

- **寿命**  
  - 実装着手後に破棄 or エンハンス  

---

## 2. 関連ファイル（アーキテクチャ層別）

| 層 | ファイルパス | 関連メソッド | モック設定 | 実装設定 |
|----|-------------|--------------|------------|----------|

### ルート層

詳細フォーマットに沿って記載  

### セクション層

### コンポーネント層

### ロジック層

### data-io層

---

## 3. データポリシー

- 静的データ中心（固定JSON / オブジェクト）  
- 擬似ランダム値は必要に応じて生成（再現性は問わない）  

---

## 4. 使用ルール

- Phase2（E2E成功後～ユニット実装前）で使用  
- AIエージェントは必要最小限のモックを生成可能  
- 人間レビュー前提で、**品質保証は対象外**
